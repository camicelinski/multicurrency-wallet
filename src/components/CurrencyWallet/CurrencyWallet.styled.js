import styled from 'styled-components'

const StyledCurrencyWallet = styled.main`
  display: flex;
  justify-content: space-between;
  flex-wrap: nowrap;
  flex-direction: column-reverse;
`

export default StyledCurrencyWallet
